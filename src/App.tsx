import { BrowserRouter } from "react-router-dom";
import { Home, CreatePost } from "./pages";
import { logo } from "./assets";

const App = () => {
  return (
    <BrowserRouter>
      <header className="w-full flex ju"></header>
    </BrowserRouter>
  );
};

export default App;
